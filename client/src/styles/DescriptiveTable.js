import styled from 'styled-components';

const Table = styled.table`
  margin: 0.6rem 0 0.8rem;
  text-align: left;
`;

const LightTd = styled.td`color: #777;`;

export default {
  Table,
  LightTd,
};
